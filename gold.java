package BAB7.definisi;
public class gold extends anggota {
    gold(double a, double b, double c, double d, double e) {
        super(a -= a * 0.15, b -= b * 0.15, c -= c * 0.15, d -= d * 0.15, e -= e* 0.15);
    }
}
