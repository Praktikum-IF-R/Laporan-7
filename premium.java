package BAB7.definisi;
public class premium extends anggota {
    premium(double a, double b, double c, double d, double e) {
         super(a -= a*0.2, b -= b*0.2, c -= c*0.2, d -= d*0.2, e -= e*0.2);
    }
}
