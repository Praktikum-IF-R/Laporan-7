package BAB7.definisi;
public class silver extends anggota {
    silver(double a, double b, double c, double d, double e) {
        super(a -= a * 0.1, b -= b * 0.1, c -= c * 0.1, d -= d * 0.1, e -= e * 0.1);
    }
}
